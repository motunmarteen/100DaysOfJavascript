// Write a program  that checks if a user is eligible to vote (age 18 and above).

let votersAge = Number(prompt("How old are you?"));

if (votersAge >= 18) {
    alert("You are Eligible to vote")
} else {
    alert("You are not Eligible to vote")
}